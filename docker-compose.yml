version: "2"
services:
    databases:
        image: 'mcr.microsoft.com/azure-sql-edge'
        ports:
            - '1433:1433'
        environment:
            - ACCEPT_EULA=1
            - MSSQL_SA_PASSWORD=EasvEasv123!
            - MSSQL_PID=Developer
    API:
        image: ksbackend
        ports:
            - '80:80'

